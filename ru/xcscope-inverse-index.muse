#title Xcscope inverse index option

Xcscope version 0.96 doesn't support inverse index, which is
absolutely required when you plat to mess with kernel source
tree. I've done [[xcscope-inv-index.patch][a patch]] which fixes that
